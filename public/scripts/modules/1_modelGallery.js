import*as THREE from"three";console.log("THREE object is loaded 😄, here is the THREE object: ",THREE);import{initScene}from"./sceneSetup.js";import{initControls}from"./controls.js";import{addObjects}from"./objects.js";import{animate}from"./animation.js";import{audioPause,audioSetup,audioStart,audioStop}from"./musicSetup.js";import{MapControls}from"three/addons/controls/MapControls.js";import{playButtonSetup}from"./menu.js";const{scene,camera,renderer}=initScene();console.log(scene,camera,renderer),audioSetup(camera),initControls(camera,renderer);export function onWindowResize(){return camera.aspect=window.innerWidth/window.innerHeight,camera.updateProjectionMatrix(),renderer.setSize(window.innerWidth,window.innerHeight),camera}window.addEventListener("resize",onWindowResize,!1),addObjects(scene,(e=>{animate(scene,camera,renderer,e)})).catch((e=>{console.error("An error occurred loading objects:",e)}));const controls=new MapControls(camera,renderer.domElement);controls.enableDamping=!0,playButtonSetup(controls);